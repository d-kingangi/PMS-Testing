#!/bin/bash

# Install dependencies
npm install --save bcrypt concurrently dotenv express joi jsonwebtoken mssql nodemon supertest uuid

# Install devDependencies
npm install --save-dev @types/bcrypt @types/concurrently @types/dotenv @types/express @types/joi @types/jsonwebtoken @types/mssql @types/nodemon @types/supertest @types/uuid @types/jest jest ts-jest typescript


